# sources:
  # file/directory
  # delta table (databricks)
  # kafka
  # other connectors -> external connect

# sinks:
  # file/directory
  # delta table
  # kafka
  # foreach - only append mode, MongoDb, snowflake, redis
  # other connectors
